version: '3.8'

services:
  database:
    image: postgres
    restart: always
    container_name: database
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: gogoro
    ports:
      - "5234:5432"
